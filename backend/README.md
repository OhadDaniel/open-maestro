# Open Maestro — backend (offline tooling)

Runs on our machine only, never per user (keeps the $0 rule). Houses the baking pipeline, the TutorBench judge, and the autoresearch loop. Imports the content schema from `../frontend/src/content` as the single source of truth.
